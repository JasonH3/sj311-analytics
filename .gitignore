# Raw and intermediate data are too large / regenerable for git — see src/download_data.py
data/raw/*.csv
data/raw/supplementary/*.geojson
data/processed/*.csv
data/processed/*.db
*.sqlite3

# Tableau working files in the repo root (the canonical workbook is
# tableau/sj311_dashboard.twb; extracts/.twbx are data, not source)
/*.twb
/*.twbx
*.twbr
*.hyper
.~*

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store
