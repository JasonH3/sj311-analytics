# San Jose 311 Service Request Analysis

Status: in progress — full write-up lands in Phase 8.

Repo structure:
- `data/` — raw (gitignored) and processed data
- `src/` — reusable Python modules (acquisition, cleaning, DB load)
- `sql/` — schema DDL and analysis queries
- `notebooks/` — EDA and statistical analysis
- `tableau/` — dashboard extract and workbook
- `docs/` — supporting write-ups (data quality log, methodology notes)
